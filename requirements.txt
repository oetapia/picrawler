# PiCrawler Python Dependencies
# Install with: pip install -r requirements.txt

# Core robot_hat library (SunFounder official package)
# IMPORTANT: Must use robot-hat (hyphen), NOT robot_hat (underscore)
# The underscore version is a different package by a different author!
# Install from GitHub for latest version with reset_mcu support
git+https://github.com/sunfounder/robot-hat.git@v2.0

# Keyboard input handling
readchar

# OLED Display
adafruit-circuitpython-ssd1306
Pillow

# I2C and GPIO
smbus2
smbus  # Also required by some robot-hat modules
RPi.GPIO
gpiozero  # Required by SunFounder robot-hat v2.0

# Audio (TTS, music playback)
pyaudio  # Note: requires 'sudo apt-get install portaudio19-dev' first
pygame   # Required by robot-hat v2.0 for sound playback

# VL53L0X ToF sensor - Built from source in setup_picrawler.sh
# (pip package doesn't work, requires pimoroni/VL53L0X-python)

# Flask for REST API
flask
flask-cors

# Video conversion (optional)
# opencv-python  # Heavy, only install if needed
